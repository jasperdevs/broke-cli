# Project Instructions

- After each code or repo change, commit the completed work and push it to GitHub before ending the turn.
- Do not leave verified changes only in the local worktree when a push is possible.
- Exclude local runtime noise such as `.omx/` state/log files and `.tmp/` scratch work from commits unless the user explicitly asks for them.
